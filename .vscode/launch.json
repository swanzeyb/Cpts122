{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C/C++: clang++ build and debug active file",
      "type": "lldb",
      "request": "launch",
      "program": "${fileWorkspaceFolder}/${relativeFileDirname}/prgrm.out",
      "cwd": "${fileWorkspaceFolder}/${relativeFileDirname}",
      "args": [],
      "env": { "TERM": "xterm" },
      "terminal": "integrated", // Or integrated | external
      "preLaunchTask": "C/C++: clang++ build active file"
    }
  ]
}
